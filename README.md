simple neural network code in lua i made for fun. doesn't have backpropagation, instead training through a user defined fitness and mutations. most of the code is self explanatory.
run `lua main.lua` inside the repository folder
